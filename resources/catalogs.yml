resources:
  schemas:
    prod:
      name: ${var.schema}
      comment: Schema for the AdventureWorks data warehouse dimensional model
      catalog_name: ${var.catalog}
      grants:
        - principal: REDACTED_DEPLOYMENT_USER_NAME
          privileges:
            - MANAGE
        - principal: ${var.deployment_service_principal_name}
          privileges:
            - MANAGE
